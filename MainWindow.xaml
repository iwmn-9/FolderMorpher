<Window x:Class="AstraSize.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AstraSize"
        xmlns:views="clr-namespace:AstraSize.Views"
        xmlns:models="clr-namespace:AstraSize.Models"
        xmlns:eff="clr-namespace:FolderMorpher.Models"
        mc:Ignorable="d"
        Title="FolderMorpher - Storage Monitor &amp; Migration Studio"
        Height="920" Width="1480"
        MinHeight="720" MinWidth="1180"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource CanvasBackground}"
        FontFamily="{StaticResource MainFont}"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.ClearTypeHint="Enabled"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <!-- Collapsible Navigation Sidebar -->
            <ColumnDefinition Width="*"/>    <!-- Main Content Work Area -->
        </Grid.ColumnDefinitions>

        <!-- 1. LEFT COLLAPSIBLE NAVIGATION SIDEBAR -->
        <Border Grid.Column="0" x:Name="SidebarBorder" Width="220" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="56"/>   <!-- Header (Brand & Toggle) -->
                    <RowDefinition Height="*"/>    <!-- Nav List -->
                    <RowDefinition Height="Auto"/> <!-- Footer / Version -->
                </Grid.RowDefinitions>

                <!-- Sidebar Header -->
                <Border Grid.Row="0" x:Name="SidebarHeaderBorder" BorderBrush="#F1F5F9" BorderThickness="0,0,0,1" Padding="12,0">
                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="SidebarToggleCol" Width="36"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="SidebarToggleButton" Content="☰" Width="34" Height="34" HorizontalAlignment="Center"
                                Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#475569"
                                Cursor="Hand" Click="SidebarToggleButton_Click" ToolTip="サイドバーの開閉 (収縮 / 展開)"/>
                        <StackPanel x:Name="SidebarBrandPanel" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                            <Border Background="#EFF6FF" CornerRadius="6" Width="28" Height="28" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock Text="📁" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="FolderMorpher" FontWeight="Bold" FontSize="14" Foreground="#0F172A"/>
                                <TextBlock Text="Storage &amp; Migration" FontSize="11" Foreground="#94A3B8"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Nav Items -->
                <StackPanel Grid.Row="1" x:Name="SidebarNavPanel" Margin="10,14">
                    <RadioButton x:Name="NavTabStorage" Style="{StaticResource SidebarNavButton}"
                                 IsChecked="True" Checked="NavTab_Checked" Tag="📊" Content="容量分析" ToolTip="容量分析"/>
                    <RadioButton x:Name="NavTabSearch" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🔍" Content="ファイル検索" ToolTip="ファイル検索"/>
                    <RadioButton x:Name="NavTabLiveAcl" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🛡️" Content="権限コントロール" ToolTip="権限コントロール"/>
                    <RadioButton x:Name="NavTabSimulation" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🚀" Content="移行スタジオ" ToolTip="移行シミュレーション"/>
                    <RadioButton x:Name="NavTabLinkFix" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🔗" Content="リンク修復" ToolTip="ショートカット・リンク修復"/>
                    <RadioButton x:Name="NavTabAudit" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🧹" Content="ファイル監査" ToolTip="重複・休眠・パス長チェック"/>
                    <RadioButton x:Name="NavTabMedia" Style="{StaticResource SidebarNavButton}"
                                 Checked="NavTab_Checked" Tag="🖼️" Content="メディア最適化" ToolTip="写真軽量化・動画抽出"/>
                </StackPanel>

                <!-- Sidebar Footer -->
                <Border Grid.Row="2" BorderBrush="#F1F5F9" BorderThickness="0,1,0,0">
                    <Grid>
                        <!-- Expanded Footer (2-tier layout: Brand on top, Controls below) -->
                        <StackPanel x:Name="SidebarFooterExpandedPanel" Margin="14,8,14,10">
                            <StackPanel Margin="0,0,0,8">
                                <TextBlock x:Name="SidebarVersionText" Text="FolderMorpher v2.2.5" FontSize="11" FontWeight="SemiBold" Foreground="#64748B"/>
                                <TextBlock x:Name="SidebarSubtitleText" Text="Storage &amp; Migration Studio" FontSize="10" Foreground="#94A3B8" Margin="0,1,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="SettingsButton" Content="⚙️" Height="24" Width="28" FontSize="11" Margin="0,0,6,0" Padding="0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="SettingsButton_Click" ToolTip="環境設定 / Settings"/>
                                <Button x:Name="LanguageToggleButton" Content="🌐 EN" Height="24" FontSize="11" FontWeight="Bold" Foreground="#2563EB" Padding="8,2"
                                        Style="{StaticResource FluentButtonSecondary}" Click="LanguageToggleButton_Click" ToolTip="言語切り替え / Switch Language (JA / EN)"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Collapsed Footer (Mini Vertical Stack) -->
                        <StackPanel x:Name="SidebarFooterCollapsedPanel" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,6,0,10">
                            <Button x:Name="SettingsButtonMini" Content="⚙️" Width="34" Height="26" FontSize="11" Margin="0,0,0,4"
                                    Style="{StaticResource FluentButtonSecondary}" Click="SettingsButton_Click" ToolTip="環境設定 / Settings"/>
                            <Button x:Name="LanguageToggleButtonMini" Content="🌐" Width="34" Height="26" FontSize="11" FontWeight="Bold" Foreground="#2563EB"
                                    Style="{StaticResource FluentButtonSecondary}" Click="LanguageToggleButton_Click" ToolTip="言語切り替え / Switch Language"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- 2. RIGHT MAIN WORK AREA -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>    <!-- Tab Views Container -->
                <RowDefinition Height="Auto"/> <!-- Global Footer / Status Bar -->
            </Grid.RowDefinitions>

            <!-- TAB VIEWS CONTAINER -->
            <Grid Grid.Row="0">
            <!-- ======================================================== -->
            <!-- TAB 1: STORAGE EXPLORER (容量分析 & マルチタブ)           -->
            <!-- ======================================================== -->
            <Grid x:Name="StorageTabPanel" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Multi-Tab Header Strip -->
                    <RowDefinition Height="Auto"/> <!-- Toolbar & Path -->
                    <RowDefinition Height="Auto"/> <!-- Key Metrics Cards -->
                    <RowDefinition Height="*"/>    <!-- Split View (Tree & Insights) -->
                </Grid.RowDefinitions>

                <!-- Multi-Tab Strip -->
                <Border Grid.Row="0" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="0,0,0,1" Padding="16,6,16,0">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <StackPanel Orientation="Horizontal">
                            <ItemsControl x:Name="StorageTabsItemsControl">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type models:ScanTabModel}">
                                        <Border Margin="0,0,4,0" CornerRadius="6,6,0,0" Padding="12,6" Cursor="Hand"
                                                MouseDown="StorageTabItem_MouseDown" Tag="{Binding}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#E2E8F0"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Background" Value="#FFFFFF"/>
                                                            <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                            <Setter Property="BorderThickness" Value="1,1,1,0"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📁 " FontSize="12" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding TabTitle}" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B" VerticalAlignment="Center"/>
                                                <Button Content="✕" Margin="8,0,0,0" Width="16" Height="16" FontSize="10"
                                                        Background="Transparent" BorderThickness="0" Foreground="#94A3B8"
                                                        Click="CloseStorageTabButton_Click" Tag="{Binding}" Cursor="Hand" ToolTip="{Binding CloseTabToolTip}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button x:Name="AddStorageTabButton" Content="＋ 新しいタブ" Height="26" Margin="4,0,0,0"
                                    Style="{StaticResource FluentButtonSecondary}" Padding="8,2" FontSize="12"
                                    Click="AddStorageTabButton_Click"/>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Storage Toolbar -->
                <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Height="32" Padding="8,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="🔍 " Foreground="#94A3B8" VerticalAlignment="Center"/>
                                <TextBox x:Name="PathTextBox" Grid.Column="1" BorderThickness="0" Background="Transparent"
                                         VerticalAlignment="Center" FontSize="12.5" FontFamily="{StaticResource MonoFont}"
                                         Foreground="#0F172A" KeyDown="PathTextBox_KeyDown"/>
                            </Grid>
                        </Border>

                        <Button x:Name="StorageBrowseButton" Grid.Column="1" Content="参照..." Margin="8,0,10,0" Height="32"
                                Style="{StaticResource FluentButtonSecondary}" Click="BrowseButton_Click"/>

                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="ScanButton" Content="スキャン開始" Height="32" Margin="0,0,8,0"
                                    Style="{StaticResource FluentButtonPrimary}" Click="ScanButton_Click"/>
                            <Button x:Name="CancelButton" Content="中止" Height="32" Margin="0,0,8,0" Background="#E11D48"
                                    Style="{StaticResource FluentButtonPrimary}" Visibility="Collapsed" Click="CancelButton_Click"/>
                            <Button x:Name="ExportButton" Content="Excel / CSV 出力" Height="32" Margin="0,0,8,0"
                                    Style="{StaticResource FluentButtonSecondary}" Click="ExportButton_Click"/>
                            <Button x:Name="TabHistoryButton" Content="📈 容量推移グラフ" Height="32"
                                    Style="{StaticResource FluentButtonSecondary}" Click="HistoryButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Metrics Cards -->
                <Border Grid.Row="2" Background="{StaticResource CanvasBackground}" Padding="20,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="14,10">
                            <StackPanel>
                                <Grid>
                                    <TextBlock x:Name="StorageKpiScannedSizeTitle" Text="スキャン対象容量" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                    <Border x:Name="MftBoostBadge" Visibility="Collapsed" Background="#EFF6FF" BorderBrush="#3B82F6" BorderThickness="1" CornerRadius="4" Padding="6,1" HorizontalAlignment="Right">
                                        <TextBlock Text="⚡ MFT Boost" FontSize="11" FontWeight="Bold" Foreground="#1D4ED8"/>
                                    </Border>
                                </Grid>
                                <TextBlock x:Name="ScannedSizeTextBlock" Text="0.00 GB" FontSize="22" FontWeight="SemiBold" Typography.NumeralAlignment="Tabular" Foreground="{StaticResource TextPrimary}" Margin="0,2,0,0"/>
                                <TextBlock x:Name="TotalFilesTextBlock" Text="0 ファイル / 0 フォルダ" FontSize="11.5" Foreground="{StaticResource TextSubtle}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="14,10">
                            <StackPanel>
                                <TextBlock x:Name="StorageKpiDiffTrendTitle" Text="前回スキャンとの差分推移" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                                <TextBlock x:Name="TrendDiffTextBlock" Text="比較データなし" FontSize="22" FontWeight="SemiBold" Typography.NumeralAlignment="Tabular" Foreground="{StaticResource AccentSky}" Margin="0,2,0,0"/>
                                <TextBlock x:Name="LastScanDateTextBlock" Text="初回スキャン" FontSize="11.5" Foreground="{StaticResource TextSubtle}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <!-- Storage Split View (Tree & Insights) -->
                <Grid Grid.Row="3" Margin="20,0,20,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6.5*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="3.5*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: TreeGrid Card -->
                    <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}"
                            BorderThickness="1" CornerRadius="12" Padding="14">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.03"/>
                        </Border.Effect>
                        <DataGrid x:Name="FileTreeDataGrid"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  EnableRowVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                  ScrollViewer.CanContentScroll="True"
                                  GridLinesVisibility="None"
                                  BorderThickness="0"
                                  Background="Transparent"
                                  RowHeight="36"
                                  SelectionMode="Single"
                                  SelectionChanged="FileTreeDataGrid_SelectionChanged"
                                  MouseDoubleClick="FileTreeDataGrid_MouseDoubleClick">
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem x:Name="CtxTreeOpenExplorer" Header="📂 エクスプローラーで開く" Click="CtxOpenExplorer_Click"/>
                                    <MenuItem x:Name="CtxTreeCopyPath" Header="📋 パスをコピー" Click="CtxCopyPath_Click"/>
                                    <MenuItem x:Name="CtxTreeScanSubtree" Header="🔍 このフォルダーをルートにしてスキャン" Click="CtxScanSubtree_Click"/>
                                    <Separator/>
                                    <MenuItem x:Name="CtxTreeEditAcl" Header="🛡️ このフォルダーのNTFS権限を確認・編集" Click="CtxEditAcl_Click"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn x:Name="ColTreeName" Header="名前" Width="3.5*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:FileItemNode}">
                                            <StackPanel Orientation="Horizontal" Margin="{Binding IndentMargin}">
                                                <Button Width="16" Height="16" Margin="0,0,4,0"
                                                        Background="Transparent" BorderThickness="0"
                                                        Click="ExpandCollapseButton_Click" Tag="{Binding}"
                                                        Visibility="{Binding HasChildren, Converter={StaticResource BoolToVis}}">
                                                    <TextBlock Text="{Binding ExpandIcon}" FontSize="10" FontWeight="Bold" Foreground="{StaticResource TextSecondary}"/>
                                                </Button>
                                                <TextBlock Text="{Binding IconGlyph}" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Name}" FontWeight="{Binding FontWeight}" FontSize="12.5" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColTreeSize" Header="容量" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:FileItemNode}">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding FormattedSize}" FontFamily="{StaticResource MonoFont}" FontSize="12.5" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                                                <Border Visibility="{Binding HasDiff, Converter={StaticResource BoolToVis}}" Background="{Binding DiffBadgeBackground}" CornerRadius="3" Padding="4,1" Margin="6,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding DiffFormatted}" FontSize="10" FontWeight="Bold" Foreground="{Binding DiffBadgeForeground}"/>
                                                </Border>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColTreeShare" Header="全体占有率" Width="115">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:FileItemNode}">
                                            <Grid Margin="4,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="45"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <ProgressBar Grid.Column="0" Height="8" Minimum="0" Maximum="100" Value="{Binding SharePercentage}"
                                                             Foreground="{Binding SharePercentage, Converter={StaticResource PercentageToBarBrush}}"
                                                             Background="#E2E8F0" BorderThickness="0" VerticalAlignment="Center"
                                                             Visibility="{Binding IsDriveRoot, Converter={StaticResource InverseBoolToVis}}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding ShareFormatted}"
                                                           FontFamily="{StaticResource MonoFont}" FontSize="12" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn x:Name="ColTreeCount" Header="ファイル数" Binding="{Binding FileCount, StringFormat='{}{0:N0}'}" Width="80" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColTreeModified" Header="最終更新" Binding="{Binding LastModified, StringFormat='yyyy/MM/dd HH:mm'}" Width="120" ElementStyle="{StaticResource LeftAlignedCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE"/>

                    <!-- Right: Insights & Extensions -->
                    <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}"
                            BorderThickness="1" CornerRadius="12" Padding="16">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.03"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Top 10 Header (2行スタック構造で長大パスでも文字被りを完全防止) -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,6">
                                <TextBlock x:Name="StorageTopFilesTitleText" Text="💡 容量上位ファイル (Top 10)" FontWeight="SemiBold" FontSize="12.5" Foreground="#1E293B"/>
                                <TextBlock x:Name="InsightsTargetScopeTextBlock" Text="スコープ: 全体" FontSize="11" Foreground="#0284C7" Margin="0,2,0,0"
                                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                            </StackPanel>

                            <!-- Top 10 Largest Files DataGrid -->
                            <DataGrid x:Name="TopFilesDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                                      BorderThickness="0" Background="Transparent" RowHeight="28" GridLinesVisibility="None" SelectionMode="Single" Margin="0,0,0,10"
                                      MouseDoubleClick="TopFilesDataGrid_MouseDoubleClick" ToolTip="ダブルクリックでエクスプローラーを開く">
                                <DataGrid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem x:Name="CtxTopOpenExplorer" Header="📂 エクスプローラーで表示" Click="CtxTopFileOpenExplorer_Click"/>
                                        <MenuItem x:Name="CtxTopCopyPath" Header="📋 パスをコピー" Click="CtxTopFileCopyPath_Click"/>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                                <DataGrid.Columns>
                                    <DataGridTextColumn x:Name="ColTopFileName" Header="ファイル名" Binding="{Binding Name}" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                    <DataGridTextColumn x:Name="ColTopFileSize" Header="サイズ" Binding="{Binding FormattedSize}" Width="75" ElementStyle="{StaticResource RightAlignedCell}"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- Folder Direct Children Shares Header -->
                            <StackPanel Grid.Row="2" Margin="0,8,0,6">
                                <TextBlock x:Name="StorageDirectSharesTitleText" Text="📁 選択フォルダーの内訳" FontWeight="SemiBold" FontSize="12.5" Foreground="#1E293B"/>
                                <TextBlock x:Name="StorageDirectSharesSubText" Text="" Visibility="Collapsed"/>
                            </StackPanel>

                            <!-- Folder Direct Children Shares DataGrid -->
                            <DataGrid x:Name="FolderChildSharesDataGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True"
                                      BorderThickness="0" Background="Transparent" RowHeight="28" GridLinesVisibility="None" SelectionMode="Single"
                                      MouseDoubleClick="FolderChildSharesDataGrid_MouseDoubleClick" ToolTip="ダブルクリックで該当フォルダへドリルダウン">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn x:Name="ColShareName" Header="項目名" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate DataType="{x:Type models:FolderChildShareItem}">
                                                <StackPanel Orientation="Horizontal" Margin="4,0">
                                                    <TextBlock Text="{Binding IconGlyph}" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Name}" FontWeight="Medium" FontSize="12" Foreground="#0F172A" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn x:Name="ColShareSize" Header="容量" Binding="{Binding FormattedSize}" Width="75" ElementStyle="{StaticResource RightAlignedCell}"/>
                                    <DataGridTemplateColumn x:Name="ColShareRatio" Header="直下比率" Width="105">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate DataType="{x:Type models:FolderChildShareItem}">
                                                <Grid Margin="4,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="45"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ProgressBar Grid.Column="0" Height="8" Minimum="0" Maximum="100" Value="{Binding RelativeSharePercentage}"
                                                                 Foreground="#0284C7" Background="#E2E8F0" BorderThickness="0" VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding RelativeShareFormatted}"
                                                               FontFamily="{StaticResource MonoFont}" FontSize="12" FontWeight="SemiBold"
                                                               Foreground="#0F172A" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="4,0,0,0"/>
                                                </Grid>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- ======================================================== -->
            <!-- TAB 2: LIVE ACL CONTROL (実環境 権限マネージャー)         -->
            <!-- ======================================================== -->
            <views:LiveAclStudio x:Name="LiveAclStudioControl" Visibility="Collapsed"/>
            <!-- ======================================================== -->
            <!-- TAB 3: MIGRATION SIMULATION STUDIO (FolderMorph Studio)   -->
            <!-- ======================================================== -->
            <Grid x:Name="SimulationTabPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Studio Action Toolbar -->
                    <RowDefinition Height="*"/>    <!-- 3-Pane Split View -->
                </Grid.RowDefinitions>

                <!-- Studio Action Toolbar -->
                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- Project Info -->
                            <ColumnDefinition Width="*"/>    <!-- Target Path Settings -->
                            <ColumnDefinition Width="Auto"/> <!-- Actions -->
                        </Grid.ColumnDefinitions>

                        <!-- Project info -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock Text="🚀" FontSize="15" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBox x:Name="SimProjectNameTextBox" Text="新ファイルサーバー移行設計_Ver1" FontWeight="Bold" FontSize="12"
                                     Foreground="#0F172A" BorderThickness="0" Background="Transparent" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Spacer -->
                        <Grid Grid.Column="1"/>

                        <!-- Toolbar Buttons -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="SimSaveProjectButton" Content="💾 保存" Height="32" Margin="0,0,5,0" Padding="8,4" FontSize="11"
                                    Style="{StaticResource FluentButtonSecondary}" Click="SimSaveProjectButton_Click" ToolTip="プロジェクト保存 (.fmorph)"/>
                            <Button x:Name="SimLoadProjectButton" Content="📂 読込" Height="32" Margin="0,0,5,0" Padding="8,4" FontSize="11"
                                    Style="{StaticResource FluentButtonSecondary}" Click="SimLoadProjectButton_Click" ToolTip="プロジェクト読込"/>
                            <Button x:Name="SimDiffReviewButton" Content="🔍 チェック" Height="32" Margin="0,0,5,0" Padding="10,4" FontSize="11"
                                    Background="#4F46E5" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0"
                                    Style="{StaticResource FluentButtonPrimary}" Click="SimDiffReviewButton_Click" ToolTip="変化点差分インスペクター"/>
                            <Button x:Name="SimDeploySkeletonButton" Content="🚀 スケルトン作成" Height="32" Margin="0,0,5,0" Padding="10,4" FontSize="11"
                                    Style="{StaticResource FluentButtonPrimary}" Click="SimDeploySkeletonButton_Click" ToolTip="空フォルダ階層と設計済みNTFSアクセス権を新環境へ先行展開"/>
                            <Button x:Name="SimExportScriptsButton" Content="📦 移行パッケージ" Height="32" Margin="0,0,5,0" Padding="8,4" FontSize="11"
                                    Style="{StaticResource FluentButtonSecondary}" Click="SimExportScriptsButton_Click" ToolTip="ベンダー標準 移行パッケージ生成（Wave分割・Runbook台帳・安全弁）"/>
                            <Button x:Name="SimExportExcelButton" Content="📊 Excel設計書" Height="32" Padding="8,4" FontSize="11"
                                    Style="{StaticResource FluentButtonSecondary}" Click="SimExportExcelButton_Click" ToolTip="移行設計書Excel出力"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 3-Pane Split View -->
                <!-- 4-Column Horizontal Workflow Layout -->
                <Grid Grid.Row="1" Margin="20,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.3*" MinWidth="180"/> <!-- Col 0: Pane 1: Source Explorer -->
                        <ColumnDefinition Width="10"/>                  <!-- Col 1: Splitter -->
                        <ColumnDefinition Width="3.1*" MinWidth="220"/> <!-- Col 2: Pane 2: Mock TreeView -->
                        <ColumnDefinition Width="10"/>                  <!-- Col 3: Splitter -->
                        <ColumnDefinition Width="3.2*" MinWidth="240"/> <!-- Col 4: Pane 3: Folder Inspector -->
                        <ColumnDefinition Width="10"/>                  <!-- Col 5: Splitter -->
                        <ColumnDefinition Width="2.4*" MinWidth="180"/> <!-- Col 6: Pane 4: AD Palette -->
                    </Grid.ColumnDefinitions>

                    <!-- ============================================== -->
                    <!-- PANE 1: SOURCE SERVER EXPLORER                 -->
                    <!-- ============================================== -->
                    <Border Grid.Column="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="12" Padding="14">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock x:Name="SimSourceTitleText" Text="① 現行ファイルサーバー (移行元)" FontWeight="Bold" FontSize="12.5" Foreground="#0F172A"/>
                                <TextBlock x:Name="SimSourceSubText" Text="サブフォルダを掴んで新ツリーへ直接ドロップ" FontSize="10" Foreground="#64748B"/>
                            </StackPanel>

                            <!-- Path, Browse & Load -->
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="4" Height="28" Padding="6,0">
                                    <TextBox x:Name="SimSourcePathTextBox" BorderThickness="0" Background="Transparent"
                                             VerticalAlignment="Center" FontSize="11" FontFamily="{StaticResource MonoFont}"
                                             KeyDown="SimSourcePathTextBox_KeyDown"/>
                                </Border>
                                <Button x:Name="SimSourceBrowseButton" Grid.Column="1" Content="参照..." Height="28" Margin="6,0,0,0"
                                        Style="{StaticResource FluentButtonSecondary}" Padding="10,2" Click="SimSourceBrowseButton_Click" ToolTip="現行フォルダ（UNCまたはローカル）を参照選択"/>
                            </Grid>

                            <!-- Source TreeView (Draggable, Smooth Scroll & Lazy Loading) -->
                            <Border Grid.Row="2" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="4" Background="#F8FAFC">
                                <TreeView x:Name="SimSourceTreeView" BorderThickness="0" Background="Transparent"
                                          ScrollViewer.CanContentScroll="False"
                                          SelectedItemChanged="SimSourceTreeView_SelectedItemChanged"
                                          PreviewMouseMove="SimSourceTreeView_PreviewMouseMove">
                                    <TreeView.ItemContainerStyle>
                                        <Style TargetType="{x:Type TreeViewItem}">
                                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                                            <EventSetter Event="Expanded" Handler="SimSourceTreeViewItem_Expanded"/>
                                        </Style>
                                    </TreeView.ItemContainerStyle>
                                    <TreeView.ItemTemplate>
                                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <TextBlock Text="📁 " FontSize="11"/>
                                                <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Medium"/>
                                                <TextBlock Text="{Binding FormattedSize, StringFormat=' ({0})'}" Foreground="#94A3B8" FontSize="10"/>
                                            </StackPanel>
                                        </HierarchicalDataTemplate>
                                    </TreeView.ItemTemplate>
                                </TreeView>
                            </Border>

                            <!-- Clone Button -->
                            <Button Grid.Row="3" x:Name="SimCloneSelectedButton" Content="➡️ 選択フォルダを新設配置" Height="30" Margin="0,8,0,0"
                                    Style="{StaticResource FluentButtonPrimary}" Click="SimCloneSelectedButton_Click" FontSize="11"/>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE"/>

                    <!-- ============================================== -->
                    <!-- PANE 2: MOCK TREE (新ツリー構造設計)             -->
                    <!-- ============================================== -->
                    <Border Grid.Column="2" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="12" Padding="14">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <TextBlock x:Name="SimMockTreeTitleText" Text="② 新環境 仮想フォルダツリー" FontWeight="Bold" FontSize="12.5" Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="SimMockTreeSubText" Text="💡 フォルダ行にドロップでサブ化 / クイックボタンで階層変更" FontSize="10" Foreground="#0284C7" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Button x:Name="SimUndoButton" Content="↩️ 戻る" Height="26" Style="{StaticResource FluentButtonSecondary}"
                                            FontSize="10.5" Padding="6,2" Margin="0,0,6,0" Click="SimUndoButton_Click" IsEnabled="False"
                                            ToolTip="直前のツリー変更を取り消す (Ctrl+Z)"/>
                                    <Button x:Name="SimAddRootFolderButton" Content="＋ ルート新設" Height="26" Style="{StaticResource FluentButtonSecondary}"
                                            FontSize="10.5" Padding="6,2" Click="SimAddRootFolderButton_Click"/>
                                </StackPanel>
                            </Grid>

                            <!-- Target Path & Browse (Symmetric to Pane 1) -->
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="4" Height="28" Padding="6,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="SimTargetRootLabel" Text="📁 移行先: " Foreground="#64748B" VerticalAlignment="Center" FontSize="11"/>
                                        <TextBox x:Name="SimTargetRootTextBox" Grid.Column="1" BorderThickness="0" Background="Transparent"
                                                 VerticalAlignment="Center" FontSize="11" FontFamily="{StaticResource MonoFont}" Text="\\NewServer01\Share"/>
                                    </Grid>
                                </Border>
                                <Button x:Name="SimTargetBrowseButton" Grid.Column="1" Content="参照..." Height="28" Margin="6,0,0,0"
                                        Style="{StaticResource FluentButtonSecondary}" Padding="8,2" Click="SimTargetBrowse_Click" ToolTip="移行先フォルダー（UNCまたはローカル）を参照選択"/>
                            </Grid>

                            <!-- Mock TreeView with Drop & ContextMenu (Smooth Scroll & Smooth Drag) -->
                            <Border Grid.Row="2" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Background="#FFFFFF">
                                <Grid>
                                    <TreeView x:Name="SimMockTreeView" BorderThickness="0" Background="Transparent"
                                              ScrollViewer.CanContentScroll="False"
                                              SelectedItemChanged="SimMockTreeView_SelectedItemChanged"
                                              AllowDrop="True"
                                              Drop="SimMockTreeView_Drop"
                                              DragOver="SimMockTreeView_DragOver"
                                              DragLeave="SimMockTreeView_DragLeave"
                                              PreviewMouseLeftButtonDown="SimMockTreeView_PreviewMouseLeftButtonDown"
                                              PreviewMouseMove="SimMockTreeView_PreviewMouseMove">
                                        <TreeView.ItemContainerStyle>
                                            <Style TargetType="{x:Type TreeViewItem}">
                                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                                            </Style>
                                    </TreeView.ItemContainerStyle>
                                    <TreeView.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem x:Name="SimCtxNewSubfolder" Header="📁 新規サブフォルダ作成" Click="CtxSimAddChild_Click"/>
                                            <MenuItem x:Name="SimCtxRename" Header="✏️ フォルダ名の変更" Click="CtxSimRename_Click"/>
                                            <Separator/>
                                            <MenuItem x:Name="SimCtxPromoteRoot" Header="⏮ 第1階層（ルート）へ昇格" Click="CtxSimJumpRoot_Click"/>
                                            <MenuItem x:Name="SimCtxPromote" Header="◀ 1階層昇格" Click="CtxSimPromote_Click"/>
                                            <MenuItem x:Name="SimCtxDemote" Header="▶ 1階層降格 (サブ化)" Click="CtxSimDemote_Click"/>
                                            <Separator/>
                                            <MenuItem x:Name="SimCtxDelete" Header="🗑️ 削除" Click="CtxSimDelete_Click"/>
                                        </ContextMenu>
                                    </TreeView.ContextMenu>
                                    <TreeView.ItemTemplate>
                                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                            <Border CornerRadius="4" Padding="4,3" Margin="0,1" SnapsToDevicePixels="True">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDragOverTarget}" Value="True">
                                                                <Setter Property="Background" Value="#E0F2FE"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid Margin="{Binding IndentMargin}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/> <!-- Icon & Name -->
                                                        <ColumnDefinition Width="Auto"/> <!-- Level Pill -->
                                                        <ColumnDefinition Width="Auto"/> <!-- Mapping Badge -->
                                                        <ColumnDefinition Width="*"/>    <!-- Quick Level Buttons -->
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="📁 " FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12" Foreground="#0F172A" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <Border Grid.Column="1" Margin="6,0,0,0" CornerRadius="3" Padding="4,1"
                                                            Background="{Binding LevelPillBackground}" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding LevelPillText}" FontSize="9.5" FontWeight="SemiBold"
                                                                   Foreground="{Binding LevelPillForeground}"/>
                                                    </Border>

                                                    <Border Grid.Column="2" Margin="4,0,0,0" CornerRadius="3" Padding="4,1" Background="#FEF3C7" BorderBrush="#FDE68A" BorderThickness="1"
                                                            VerticalAlignment="Center" Visibility="{Binding HasMapping, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="{Binding MappingBadgeText}" FontSize="9.5" Foreground="#B45309"/>
                                                    </Border>

                                                    <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                        <Button Content="⏮" Width="18" Height="18" Padding="0" FontSize="9" ToolTip="{Binding QuickJumpRootToolTip}"
                                                                Style="{StaticResource FluentButtonSecondary}" Margin="1,0" Click="SimQuickJumpRoot_Click" Tag="{Binding}"/>
                                                        <Button Content="◀" Width="18" Height="18" Padding="0" FontSize="9" ToolTip="{Binding QuickPromoteToolTip}"
                                                                Style="{StaticResource FluentButtonSecondary}" Margin="1,0" Click="SimQuickPromote_Click" Tag="{Binding}"/>
                                                        <Button Content="▶" Width="18" Height="18" Padding="0" FontSize="9" ToolTip="{Binding QuickDemoteToolTip}"
                                                                Style="{StaticResource FluentButtonSecondary}" Margin="1,0" Click="SimQuickDemote_Click" Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </HierarchicalDataTemplate>
                                    </TreeView.ItemTemplate>
                                </TreeView>

                                <!-- Empty State Indicator -->
                                <Border x:Name="SimEmptyStateBorder" Background="Transparent" IsHitTestVisible="False"
                                        VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20" Visibility="Collapsed">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="📁" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,8" Opacity="0.6"/>
                                        <TextBlock x:Name="SimEmptyStateTitleText" Text="仮想ツリーが未作成です" FontWeight="SemiBold" FontSize="12.5" Foreground="#64748B" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="SimEmptyStateDescText" Text="左側の現行フォルダを選択して『新設配置』を押すか、&#x0a;上部の『＋ ルート新設』ボタンから設計を開始してください。"
                                                   FontSize="11" Foreground="#94A3B8" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,0" LineHeight="16"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="3" Width="10" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE"/>

                    <!-- ============================================== -->
                    <!-- PANE 3: FOLDER INSPECTOR (マッピング ＆ 権限)     -->
                    <!-- ============================================== -->
                    <Border Grid.Column="4" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="12" Padding="14">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/> <!-- Selected Folder Info -->
                                <RowDefinition Height="*"/>    <!-- Inspector Scrollable Area -->
                            </Grid.RowDefinitions>

                            <!-- Row 0: Selected Folder Header -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock x:Name="SimInspectorTitleText" Text="③ フォルダ詳細 ＆ 権限設定" FontWeight="Bold" FontSize="12.5" Foreground="#0F172A"/>
                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <TextBlock x:Name="SimSelectedFolderPrefixText" Text="📁 " FontWeight="Bold" FontSize="12" Foreground="#334155"/>
                                        <TextBlock x:Name="SimSelectedFolderNameText" Text="(未選択 - 左のフォルダをクリック)" FontWeight="Bold" FontSize="12" Foreground="#0284C7" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" x:Name="SimInheritCheckBox" Content="親の権限継承" HorizontalAlignment="Right" VerticalAlignment="Center"
                                              FontSize="11" Checked="SimInheritCheckBox_Changed" Unchecked="SimInheritCheckBox_Changed"/>
                                </Grid>
                            </StackPanel>

                            <!-- Row 1: Inspector Scrollable Area (広域ドロップ受容) -->
                            <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10"
                                    AllowDrop="True" Drop="SimAccordion_Drop" DragOver="SimGenericDragOver">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <!-- Drop Zone 2: N:1 Source Mapping Area -->
                                        <Border Background="#FAF5FF" BorderBrush="#C084FC" BorderThickness="1"
                                                CornerRadius="4" Padding="8" Margin="0,0,0,8" AllowDrop="True"
                                                Drop="SimMappingDropZone_Drop" DragOver="SimGenericDragOver">
                                            <StackPanel>
                                                <Grid Margin="0,0,0,4">
                                                    <TextBlock x:Name="SimMappingTitleText" Text="🔗 移行元マッピング (データ移行元 / N:1統合)" FontSize="11" FontWeight="Bold" Foreground="#7E22CE"/>
                                                </Grid>
                                                <!-- List of mapped sources -->
                                                <ItemsControl x:Name="SimMappedSourcesItemsControl">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="#FFFFFF" BorderBrush="#E9D5FF" BorderThickness="1" CornerRadius="4" Padding="6,3" Margin="0,2"
                                                                    Cursor="Hand" MouseDown="SimMappedSource_MouseDown" PreviewMouseMove="SimMappedSource_PreviewMouseMove">
                                                                <TextBlock Text="{Binding StringFormat='🔗 {0}'}" FontSize="10.5" FontFamily="{StaticResource MonoFont}" Foreground="#4C1D95" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>

                                        <!-- Drop Zone 3: AD Permissions Cards Area -->
                                        <Border Background="#F0F9FF" BorderBrush="#38BDF8" BorderThickness="1"
                                                CornerRadius="4" Padding="8" AllowDrop="True"
                                                Drop="SimAclDropZone_Drop" DragOver="SimGenericDragOver">
                                            <StackPanel>
                                                <Grid Margin="0,0,0,4">
                                                    <TextBlock x:Name="SimAclTitleText" Text="🛡️ 割り当てられたADアクセス権" FontSize="11" FontWeight="Bold" Foreground="#0369A1"/>
                                                    <Button x:Name="SimOpenSecModalButton" Content="⚙️ 詳細設定" FontSize="10" Height="20" Padding="5,0"
                                                            Style="{StaticResource FluentButtonSecondary}" HorizontalAlignment="Right"
                                                            Click="SimOpenSecModalButton_Click"/>
                                                </Grid>
                                                <!-- Permissions Cards Grid (縦リストで視認性向上) -->
                                                <ItemsControl x:Name="SimAclCardsItemsControl">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Vertical"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate DataType="{x:Type models:SimAclEntry}">
                                                            <Border Background="#FFFFFF" BorderBrush="#BAE6FD" BorderThickness="1" CornerRadius="4"
                                                                    Padding="8,5" Margin="0,0,0,4" Cursor="Hand"
                                                                    MouseDown="SimAclCard_MouseDown"
                                                                    PreviewMouseMove="SimAclCard_PreviewMouseMove">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto"/>
                                                                        <RowDefinition Height="Auto"/>
                                                                    </Grid.RowDefinitions>
                                                                    <!-- Row 0: アイコン ＋ 表示名 (横幅いっぱい) -->
                                                                    <Grid Grid.Row="0" Margin="0,0,0,3">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="Auto"/>
                                                                            <ColumnDefinition Width="*"/>
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBlock Grid.Column="0" Text="{Binding IconGlyph}" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="Bold" FontSize="11.5" Foreground="#0F172A" 
                                                                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding DisplayName}"/>
                                                                    </Grid>
                                                                    <!-- Row 1: 権限バッジ ＋ 適用先バッジ -->
                                                                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                                                        <Border Background="#E0F2FE" CornerRadius="3" Padding="5,1.5" Margin="0,0,4,0" VerticalAlignment="Center">
                                                                            <TextBlock Text="{Binding FormattedRights}" FontSize="10" Foreground="#0284C7"/>
                                                                        </Border>
                                                                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="3" Padding="5,1.5" VerticalAlignment="Center">
                                                                            <TextBlock Text="{Binding AppliesTo}" FontSize="9.5" Foreground="#64748B"/>
                                                                        </Border>
                                                                    </StackPanel>
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="5" Width="10" HorizontalAlignment="Stretch" Background="Transparent" Cursor="SizeWE"/>

                    <!-- ============================================== -->
                    <!-- PANE 4: AD PALETTE (USERS & GROUPS)            -->
                    <!-- ============================================== -->
                    <Border Grid.Column="6" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="1" CornerRadius="12" Padding="14">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                        </Border.Effect>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 0: Title & Refresh Button -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock x:Name="SimAdHeaderTitle" Text="④ AD パレット" FontWeight="Bold" FontSize="12" Foreground="#0F172A" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock x:Name="SimAdHeaderSubText" Text="カードを中央のフォルダまたは詳細へドラッグ" FontSize="10" Foreground="#64748B" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <Button x:Name="SimAdRefreshButton" Grid.Column="1" Content="🔄" Width="22" Height="22" Padding="0" FontSize="11"
                                            Style="{StaticResource FluentButtonSecondary}" ToolTip="ADアカウントを今すぐ最新に更新"
                                            VerticalAlignment="Top" Click="SimAdRefreshButton_Click"/>
                                </Grid>

                                <!-- Row 1: Badges WrapPanel -->
                                <WrapPanel Grid.Row="1" Margin="0,6,0,0" VerticalAlignment="Center">
                                    <Border x:Name="DomainStatusBadge" Background="#DCFCE7" CornerRadius="4" Padding="6,1" Margin="0,0,6,4" VerticalAlignment="Center"
                                            ToolTip="接続中のActive Directoryドメイン">
                                        <TextBlock x:Name="DomainStatusText" Text="🟢 CORP.LOCAL" FontSize="9.5" FontWeight="SemiBold" Foreground="#15803D"/>
                                    </Border>
                                    <Border x:Name="SimAdSyncBadgeBorder" Background="#DCFCE7" CornerRadius="4" Padding="6,1" Margin="0,0,6,4" VerticalAlignment="Center"
                                            ToolTip="30秒ごとにドメイン/ローカルのアカウント変化を自動チェック">
                                        <TextBlock x:Name="SimAdSyncBadgeText" Text="🟢 自動同期 (30s)" FontSize="9.5" FontWeight="SemiBold" Foreground="#15803D"/>
                                    </Border>
                                </WrapPanel>
                            </Grid>

                            <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Height="30" Padding="6,0" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="🔍 " FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center"/>
                                    <TextBox x:Name="AdSearchTextBox" Grid.Column="1" BorderThickness="0" Background="Transparent"
                                             VerticalAlignment="Center" FontSize="11" TextChanged="AdSearchTextBox_TextChanged"/>
                                </Grid>
                            </Border>

                            <Border Grid.Row="2" BorderBrush="#F1F5F9" BorderThickness="1" CornerRadius="4">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl x:Name="AdPrincipalsItemsControl">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:AdPrincipalItem}">
                                                <Border Background="{Binding BadgeBackground}" BorderBrush="#E2E8F0" BorderThickness="1"
                                                        CornerRadius="6" Padding="8,5" Margin="0,0,0,4" Cursor="Hand"
                                                        PreviewMouseMove="AdCard_PreviewMouseMove" Tag="{Binding}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        <!-- Row 0: アイコン ＋ 表示名 (横幅100%活用) -->
                                                        <Grid Grid.Row="0" Margin="0,0,0,2">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding IconGlyph}" FontSize="12.5" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="11.5" Foreground="#0F172A" 
                                                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding DisplayName}"/>
                                                        </Grid>
                                                        <!-- Row 1: アカウント名 ＋ 種別バッジ -->
                                                        <Grid Grid.Row="1">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding AccountName}" FontSize="10" Foreground="#64748B" 
                                                                       TextTrimming="CharacterEllipsis" ToolTip="{Binding AccountName}" VerticalAlignment="Center"/>
                                                            <Border Grid.Column="1" Background="{Binding TypeBadgeBg}" CornerRadius="3" Padding="4,1" VerticalAlignment="Center" Margin="4,0,0,0">
                                                                <TextBlock Text="{Binding TypeBadgeText}" FontSize="9.5" Foreground="{Binding TypeBadgeFg}"/>
                                                            </Border>
                                                        </Grid>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- ======================================================== -->
            <!-- TAB 3: LINK FIXER (ショートカット＆リンク一括修復)        -->
            <!-- ======================================================== -->
            <Grid x:Name="LinkFixTabPanel" Visibility="Collapsed" Margin="20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,16">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,14">
                            <TextBlock x:Name="LinkFixHeaderTitle" Text="🔗 サーバー移行に伴うショートカット (LNK) / パス参照の一括修復" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock x:Name="LinkFixHeaderDesc" Text="ファイルサーバー移行後、旧パス（例: \\OldServer\Share）を指したまま切断されたショートカットを一括検出し、新パスへ安全に書き換えます。"
                                       FontSize="11" Foreground="#64748B" Margin="0,4,0,0"/>
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock x:Name="LinkSearchScopeLabel" Text="検索対象フォルダ (デスクトップ等)" FontSize="11" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="34" Padding="10,0">
                                    <TextBox x:Name="LinkSearchScopeTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="11" FontFamily="{StaticResource MonoFont}"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                <TextBlock x:Name="LinkOldPatternLabel" Text="置換前 (旧サーバー名 / パス)" FontSize="11" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="34" Padding="10,0">
                                    <TextBox x:Name="LinkOldPatternTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="11" FontFamily="{StaticResource MonoFont}"/>
                                </Border>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock x:Name="LinkNewPatternLabel" Text="置換後 (新サーバー名 / パス)" FontSize="11" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="34" Padding="10,0">
                                    <TextBox x:Name="LinkNewPatternTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="11" FontFamily="{StaticResource MonoFont}"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox x:Name="LinkIncludeOfficeCheckBox" Content="Officeファイル内部リンク (.xlsx/.xlsm) も対象に含める" IsChecked="True" VerticalAlignment="Center" FontSize="11" Foreground="#475569"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="LinkGenerateGpoButton" Content="📜 GPOログオンスクリプト生成 (.ps1)" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="LinkGenerateGpoButton_Click" ToolTip="全社PCのデスクトップ/マイドキュメント等のショートカットを自動修復するスクリプトを出力"/>
                                <Button x:Name="LinkScanButton" Content="切断リンク検出スキャン" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="LinkScanButton_Click"/>
                                <Button x:Name="LinkFixExecuteButton" Content="🔍 チェック" Height="32"
                                        Style="{StaticResource FluentButtonPrimary}" Click="LinkFixExecuteButton_Click"
                                        ToolTip="修復対象のショートカット一覧と置換差分をチェック"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="18">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="LinkTableTitleText" Grid.Row="0" Text="検出されたショートカット一覧" FontWeight="Bold" FontSize="12" Foreground="#0F172A" Margin="0,0,0,10"/>
                        <DataGrid x:Name="LinkItemsDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="30" SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn x:Name="ColLinkFileName" Header="ファイル名" Binding="{Binding FileName}" Width="160" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColLinkFileType" Header="種別" Binding="{Binding FileType}" Width="120" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColLinkOldTarget" Header="現在のターゲットパス" Binding="{Binding OldTarget}" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColLinkNewTarget" Header="置換後ターゲットパス" Binding="{Binding NewTarget}" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColLinkStatus" Header="状態" Binding="{Binding Status}" Width="130" ElementStyle="{StaticResource LeftAlignedCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- ======================================================== -->
            <!-- TAB 5: AUDIT & HYGIENE (断捨離・ファイルサーバー健全化)       -->
            <!-- ======================================================== -->
            <Grid x:Name="AuditTabPanel" Visibility="Collapsed" Margin="20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Controls Card -->
                    <RowDefinition Height="Auto"/> <!-- KPI Summary Cards -->
                    <RowDefinition Height="*"/>    <!-- Results DataGrid -->
                </Grid.RowDefinitions>

                <!-- Controls Card -->
                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,14">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock x:Name="AuditHeaderTitle" Text="🧹 ファイルサーバー健全化 ＆ ファイル監査（重複整理・容量削減）" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock x:Name="AuditHeaderDesc" Text="重複ファイル (SHA256)、休眠ファイル (3年以上未更新)、パス長240文字超、移行禁則文字を一括抽出し、安全な棚卸し台帳を出力します。"
                                       FontSize="11" Foreground="#64748B" Margin="0,4,0,0"/>
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock x:Name="AuditTargetFolderLabel" Text="監査対象ディレクトリ (UNC / ローカル)" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="34" Padding="10,0">
                                            <TextBox x:Name="AuditPathTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12" FontFamily="{StaticResource MonoFont}"/>
                                        </Border>
                                        <Button x:Name="AuditBrowseButton" Grid.Column="1" Content="📁 参照" Width="96" Height="34" Margin="8,0,0,0"
                                                Style="{StaticResource FluentButtonSecondary}" Click="AuditBrowseButton_Click"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,4">
                                    <CheckBox x:Name="AuditCheckDuplicatesCheckBox" Content="重複ファイル (SHA256)" IsChecked="True" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="12" Foreground="#334155"/>
                                    <CheckBox x:Name="AuditCheckDormantCheckBox" Content="休眠ファイル (3年以上)" IsChecked="True" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="12" Foreground="#334155"/>
                                    <CheckBox x:Name="AuditCheckPathLimitsCheckBox" Content="パス長危険域(240字超)/禁則文字" IsChecked="True" VerticalAlignment="Center" Margin="0,0,14,0" FontSize="12" Foreground="#334155"/>
                                    <TextBlock x:Name="AuditBandwidthLabel" Text="帯域制限:" VerticalAlignment="Center" FontSize="12" Foreground="#64748B" Margin="0,0,6,0"/>
                                    <ComboBox x:Name="AuditBandwidthComboBox" Height="30" Width="175" VerticalAlignment="Center" FontSize="11.5">
                                        <ComboBoxItem x:Name="AuditBandwidthStandardItem" Content="通常 50MB/s (他業務保護)" IsSelected="True" Tag="Standard50MB"/>
                                        <ComboBoxItem x:Name="AuditBandwidthUnlimitedItem" Content="無制限 (夜間・最速)" Tag="Unlimited"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Row="1" Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="AuditExcludeFoldersLabel" Text="除外フォルダー名 (部分一致 / カンマ区切り):" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,10,0"/>
                                <Border Grid.Column="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="32" Padding="10,0">
                                    <TextBox x:Name="AuditExcludeFoldersTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12" FontFamily="{StaticResource MonoFont}"
                                             ToolTip="指定した名前を含むフォルダー全体を走査対象からスキップします (例: .git, node_modules, temp, archive)"/>
                                </Border>
                            </Grid>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="AuditStatusText" Text="待機中" FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="AuditExportExcelButton" Content="📊 Excelレポート出力 (.xlsx)" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="AuditExportExcelButton_Click" ToolTip="上司・各部署提出用の美麗Excelレポートを生成"/>
                                <Button x:Name="AuditExportCsvButton" Content="📄 CSV台帳出力" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="AuditExportCsvButton_Click"/>
                                <Button x:Name="AuditDeleteSelectedButton" Content="🗑️ 選択ファイルを完全削除" Height="32" Margin="0,0,8,0"
                                        Background="#EF4444" Foreground="#FFFFFF" FontWeight="Bold" BorderThickness="0"
                                        Style="{StaticResource FluentButtonPrimary}" Click="AuditDeleteSelectedButton_Click"
                                        ToolTip="チェックを入れたファイルを直接完全削除します（※復元不可）"/>
                                <Button x:Name="AuditStartButton" Content="🔍 監査スキャン開始" Height="32"
                                        Style="{StaticResource FluentButtonPrimary}" Click="AuditStartButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Slim 1-Row Metrics Bar (Quiet Fluent) -->
                <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,8" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- 総走査ファイル数 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- 重複の無駄 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- 休眠ファイル容量 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="*"/>    <!-- パス長・禁則違反 -->
                        </Grid.ColumnDefinitions>

                        <!-- Total Files -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="AuditKpiTotalFilesTitle" Text="総走査ファイル数:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="AuditKpiTotalFiles" Text="0 件" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 1 -->
                        <Border Grid.Column="1" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Duplicates Wasted -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="AuditKpiDupWastedTitle" Text="重複の無駄:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="AuditKpiDupWasted" Text="0 B" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#DC2626" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 2 -->
                        <Border Grid.Column="3" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Dormant Size -->
                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="AuditKpiDormantSizeTitle" Text="休眠ファイル容量:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="AuditKpiDormantSize" Text="0 B" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#D97706" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 3 -->
                        <Border Grid.Column="5" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Path Limits -->
                        <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="AuditKpiPathLimitsTitle" Text="パス長・禁則違反:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="AuditKpiPathLimits" Text="0 件" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#2563EB" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Results DataGrid -->
                <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="AuditTableTitleText" Grid.Column="0" Text="検出された課題・整理候補一覧" FontWeight="Bold" FontSize="12" Foreground="#0F172A" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Background="#FEE2E2" BorderBrush="#FCA5A5" BorderThickness="1" CornerRadius="6" Padding="8,3" Margin="14,0,0,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="AuditLiveReductionLabel" Text="選択中の削減見込み: " FontSize="11" Foreground="#991B1B" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="AuditLiveSelectedReductionText" Text="0 B (0 件)" FontSize="11" Foreground="#DC2626" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="AuditSmartSelectLabel" Text="☑️ 一括選択:" FontSize="11.5" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <ComboBox x:Name="AuditSmartSelectComboBox" Width="170" Height="28" FontSize="11" Margin="0,0,12,0" SelectionChanged="AuditSmartSelectComboBox_SelectionChanged">
                                    <ComboBoxItem x:Name="AuditSmartItemPreset" Content="選択プリセット..." IsSelected="True" Tag="None"/>
                                    <ComboBoxItem x:Name="AuditSmartItemDupCopy" Content="重複の原本以外を選択" Tag="DupCopyOnly"/>
                                    <ComboBoxItem x:Name="AuditSmartItemDormant3Y" Content="3年以上前の休眠を選択" Tag="Dormant3Y"/>
                                    <ComboBoxItem x:Name="AuditSmartItemDormant5Y" Content="5年以上前の休眠を選択" Tag="Dormant5Y"/>
                                    <ComboBoxItem x:Name="AuditSmartItemVisible" Content="表示中のみすべて選択" Tag="SelectVisible"/>
                                    <ComboBoxItem x:Name="AuditSmartItemClear" Content="選択をすべて解除" Tag="ClearAll"/>
                                </ComboBox>

                                <TextBlock x:Name="AuditFilterLabel" Text="絞り込み:" FontSize="11.5" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <ComboBox x:Name="AuditCategoryFilterComboBox" Width="135" Height="28" FontSize="11.5" Margin="0,0,10,0" SelectionChanged="AuditFilter_Changed">
                                    <ComboBoxItem Content="すべて表示" IsSelected="True" Tag="All"/>
                                    <ComboBoxItem Content="重複ファイルのみ" Tag="Duplicate"/>
                                    <ComboBoxItem Content="休眠ファイルのみ" Tag="Dormant"/>
                                    <ComboBoxItem Content="パス長・禁則のみ" Tag="PathLimit"/>
                                </ComboBox>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Height="28" Width="180" Padding="6,0">
                                    <Grid>
                                        <TextBox x:Name="AuditSearchFilterTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="11.5" TextChanged="AuditFilter_Changed"/>
                                        <TextBlock x:Name="AuditSearchPlaceholder" Text="🔍 ファイル名/パス検索..." FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center" IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, ElementName=AuditSearchFilterTextBox}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Grid>
                        <DataGrid x:Name="AuditItemsDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="30" HeadersVisibility="Column" RowHeaderWidth="0"
                                  MouseDoubleClick="AuditItemsDataGrid_MouseDoubleClick"
                                  Sorting="AuditItemsDataGrid_Sorting">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="{Binding RowBackgroundHex}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#E2E8F0"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F1F5F9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Width="38" CanUserSort="False">
                                    <DataGridTemplateColumn.Header>
                                        <CheckBox x:Name="AuditHeaderCheckBox" HorizontalAlignment="Center" VerticalAlignment="Center" Click="AuditHeaderCheckBox_Click" ToolTip="すべて選択 / すべて解除"/>
                                    </DataGridTemplateColumn.Header>
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn x:Name="ColAuditIssueType" Header="問題種別" Binding="{Binding IssueTypeDisplay}" SortMemberPath="IssueType" Width="130" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTemplateColumn x:Name="ColAuditDupGroup" Header="重複グループ" SortMemberPath="DuplicateGroupIndex" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding BadgeBackgroundHex}" BorderBrush="{Binding BadgeBorderHex}"
                                                    BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="4,2" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding DuplicateGroupBadge}" FontSize="10.5" FontWeight="SemiBold"
                                                           Foreground="{Binding BadgeForegroundHex}" VerticalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn x:Name="ColAuditFileName" Header="ファイル名" Binding="{Binding FileName}" SortMemberPath="FileName" Width="180" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColAuditSize" Header="容量" Binding="{Binding SizeFormatted}" SortMemberPath="Size" Width="90" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColAuditModified" Header="最終更新日時" Binding="{Binding LastWriteTime, StringFormat='yyyy/MM/dd HH:mm'}" SortMemberPath="LastWriteTime" Width="130" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColAuditDetail" Header="詳細" Binding="{Binding Detail}" SortMemberPath="Detail" Width="200" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColAuditFullPath" Header="完全パス" Binding="{Binding FullPath}" SortMemberPath="FullPath" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- ======================================================== -->
            <!-- TAB 6: MEDIA OPTIMIZER (写真軽量化 ＆ 巨大動画抽出)        -->
            <!-- ======================================================== -->
            <Grid x:Name="MediaTabPanel" Visibility="Collapsed" Margin="20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Controls Card -->
                    <RowDefinition Height="Auto"/> <!-- KPI Summary Cards -->
                    <RowDefinition Height="*"/>    <!-- Results DataGrid -->
                </Grid.RowDefinitions>

                <!-- Controls Card -->
                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="18" Margin="0,0,0,14">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="6" ShadowDepth="1" Direction="270" Color="#000000" Opacity="0.04"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,12">
                            <TextBlock x:Name="MediaHeaderTitle" Text="🖼️ メディア・オプティマイザ（写真・画像最適化 ＆ 大容量動画抽出）" FontSize="15" FontWeight="Bold" Foreground="#0F172A"/>
                            <TextBlock x:Name="MediaHeaderDesc" Text="保護対象（_Master、印刷用、RAW等）を自動スキップしながら、大容量写真（2MB超）を最適化（長辺2560px超は縮小/85%品質/Exif・日時保持）で上書き軽量化し、大容量動画のTop抽出と夜間圧縮バッチを出力します。"
                                       FontSize="11" Foreground="#64748B" Margin="0,4,0,0"/>
                        </StackPanel>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock x:Name="MediaTargetDirLabel" Text="走査対象ディレクトリ (UNC / ローカル)" FontSize="12" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,6"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="34" Padding="10,0">
                                        <TextBox x:Name="MediaPathTextBox" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12" FontFamily="{StaticResource MonoFont}"/>
                                    </Border>
                                    <Button x:Name="MediaBrowseButton" Grid.Column="1" Content="📁 参照" Width="96" Height="34" Margin="8,0,0,0"
                                            Style="{StaticResource FluentButtonSecondary}" Click="MediaBrowseButton_Click"/>
                                </Grid>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,4">
                                <StackPanel Margin="0,0,12,0">
                                    <TextBlock x:Name="MediaMaxDimLabel" Text="最大長辺 (px)" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Height="30" Width="80" Padding="6,0">
                                        <TextBox x:Name="MediaMaxDimTextBox" Text="2560" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Margin="0,0,12,0">
                                    <TextBlock x:Name="MediaQualityLabel" Text="画質 (%)" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Height="30" Width="60" Padding="6,0">
                                        <TextBox x:Name="MediaQualityTextBox" Text="85" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock x:Name="MediaMinSizeLabel" Text="最小サイズ (MB)" FontSize="10" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Height="30" Width="70" Padding="6,0">
                                        <TextBox x:Name="MediaMinSizeMbTextBox" Text="2" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="11"/>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="2" Margin="0,16,0,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="MediaStatusText" Text="待機中" FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="MediaExportExcelButton" Content="📊 Excelレポート出力 (.xlsx)" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="MediaExportExcelButton_Click"/>
                                <Button x:Name="MediaGenVideoBatchButton" Content="🎬 大容量動画 夜間圧縮バッチ出力 (.bat)" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="MediaGenVideoBatchButton_Click" ToolTip="GPUハードウェアエンコード (H.265) で動画を一括軽量化するスクリプトを出力"/>
                                <Button x:Name="MediaScanButton" Content="🔍 メディア走査" Height="32" Margin="0,0,8,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="MediaScanButton_Click"/>
                                <Button x:Name="MediaOptimizeButton" Content="🔍 チェック" Height="32"
                                        Style="{StaticResource FluentButtonPrimary}" Click="MediaOptimizeButton_Click"
                                        ToolTip="軽量化対象の写真一覧と設定差分をチェック"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Slim 1-Row Metrics Bar (Quiet Fluent) -->
                <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,8" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- 画像数 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- 動画数 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- 軽量化完了数 -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="*"/>    <!-- 総削減容量 -->
                        </Grid.ColumnDefinitions>

                        <!-- Images Count -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="MediaKpiImagesCountTitle" Text="走査対象 画像数:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MediaKpiImagesCount" Text="0 枚" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 1 -->
                        <Border Grid.Column="1" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Videos Count -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="MediaKpiVideosCountTitle" Text="大容量動画数:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MediaKpiVideosCount" Text="0 本" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#2563EB" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 2 -->
                        <Border Grid.Column="3" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Optimized Count -->
                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="MediaKpiOptimizedCountTitle" Text="軽量化 完了数:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MediaKpiOptimizedCount" Text="0 枚" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#059669" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 3 -->
                        <Border Grid.Column="5" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Saved Size -->
                        <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="MediaKpiSavedSizeTitle" Text="総削減容量:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="MediaKpiSavedSize" Text="0 B" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#059669" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Results DataGrid -->
                <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="MediaTableTitleText" Grid.Row="0" Text="メディア一覧（画像 ＆ 大容量動画）" FontWeight="Bold" FontSize="12" Foreground="#0F172A" Margin="0,0,0,10"/>
                        <DataGrid x:Name="MediaItemsDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="28" HeadersVisibility="Column" RowHeaderWidth="0"
                                  MouseDoubleClick="MediaItemsDataGrid_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn x:Name="ColMediaType" Header="種別" Binding="{Binding Extension}" Width="80" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaFileName" Header="ファイル名" Binding="{Binding FileName}" Width="180" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaOriginalSize" Header="元容量" Binding="{Binding OriginalSizeFormatted}" Width="100" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaOptimizedSize" Header="軽量化後" Binding="{Binding OptimizedSizeFormatted}" Width="100" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaSavedSize" Header="削減容量" Binding="{Binding SavedSizeFormatted}" Width="100" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaStatus" Header="状態 / 保護" Binding="{Binding Status}" Width="180" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColMediaFullPath" Header="完全パス" Binding="{Binding FullPath}" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- ======================================================== -->
            <!-- TAB 7: SEARCH STUDIO (高速・全文 統合ファイル検索)         -->
            <!-- ======================================================== -->
            <Grid x:Name="SearchTabPanel" Visibility="Collapsed" Margin="20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Controls Card -->
                    <RowDefinition Height="Auto"/> <!-- KPI Summary Cards -->
                    <RowDefinition Height="*"/>    <!-- Results DataGrid -->
                </Grid.RowDefinitions>

                <!-- Controls Card -->
                <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,12" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Search Box & Action Buttons -->
                            <RowDefinition Height="Auto"/> <!-- Scope Selection & Target Folder -->
                            <RowDefinition Height="Auto"/> <!-- Progress Bar -->
                        </Grid.RowDefinitions>

                        <!-- Row 0: Search Box & Buttons -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SearchInputBox" Height="36" FontSize="13" Padding="10,0"
                                     VerticalContentAlignment="Center" BorderBrush="#CBD5E1"
                                     TextChanged="SearchInputBox_TextChanged" KeyDown="SearchInputBox_KeyDown"
                                     ToolTip="検索構文を入力 (例: ext:xlsx size:>10MB dormant:>3y content:社外秘 regex:^backup.*\.zip)"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                                <Button x:Name="SearchExecuteButton" Content="🔍 検索" Height="36" Padding="16,0"
                                        Style="{StaticResource FluentButtonPrimary}" Click="SearchExecuteButton_Click"
                                        ToolTip="検索を実行 (Enterキーでも実行可能)"/>
                                <Button x:Name="SearchCancelButton" Content="⏹ 中断" Height="36" Padding="12,0" Margin="6,0,0,0"
                                        Style="{StaticResource FluentButtonDanger}" Click="SearchCancelButton_Click"
                                        Visibility="Collapsed" ToolTip="実行中の走査を中断"/>
                                <Button x:Name="SearchClearButton" Content="✕ クリア" Height="36" Padding="12,0" Margin="6,0,0,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="SearchClearButton_Click"
                                        ToolTip="入力内容と結果をクリア"/>
                            </StackPanel>
                        </Grid>

                        <!-- Row 1: Filters & Target Folder (全自動スマート検索) -->
                        <Grid Grid.Row="1" Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                                <CheckBox x:Name="SearchIncludeFoldersCheckBox" Content="📁 フォルダも含める"
                                          FontSize="12" Foreground="#475569" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,14,0"
                                          ToolTip="検索結果にフォルダーも含めます"/>
                                <CheckBox x:Name="SearchContentCheckBox" Content="📄 本文も検索"
                                          FontSize="12" Foreground="#2563EB" FontWeight="SemiBold" VerticalAlignment="Center"
                                          ToolTip="ファイル本文（Office/PDF/テキスト等）も探索します"/>
                            </StackPanel>
                            <TextBlock x:Name="SearchTargetFolderLabel" Grid.Column="1" Text="対象フォルダー:" FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <Grid Grid.Column="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="SearchDirectTargetTextBox" Height="28" FontSize="11" Padding="6,0" VerticalContentAlignment="Center" BorderBrush="#CBD5E1"/>
                                <Button x:Name="SearchDirectBrowseButton" Grid.Column="1" Content="📁 参照..." Height="28" Margin="4,0,0,0"
                                        Style="{StaticResource FluentButtonSecondary}" Click="SearchDirectBrowseButton_Click" ToolTip="走査対象フォルダーを選択"/>
                            </Grid>
                        </Grid>

                        <!-- Row 2: Progress Bar -->
                        <ProgressBar x:Name="SearchProgressBar" Grid.Row="2" Height="3" Margin="0,8,0,0"
                                     IsIndeterminate="True" Visibility="Collapsed" Foreground="#2563EB" Background="#E2E8F0" BorderThickness="0"/>
                    </Grid>
                </Border>

                <!-- Slim 1-Row Metrics Bar (Quiet Fluent) -->
                <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16,8" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- Hit count -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- Total size -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="Auto"/> <!-- Elapsed -->
                            <ColumnDefinition Width="Auto"/> <!-- Separator -->
                            <ColumnDefinition Width="*"/>    <!-- Status -->
                        </Grid.ColumnDefinitions>

                        <!-- Hit Count -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="SearchKpiHitCountTitle" Text="ヒット件数:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="SearchKpiHitCountText" Text="0 件" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#0F172A" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 1 -->
                        <Border Grid.Column="1" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Total Size -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="SearchKpiTotalSizeTitle" Text="合計容量:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="SearchKpiTotalSizeText" Text="0 B" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#2563EB" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 2 -->
                        <Border Grid.Column="3" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Elapsed -->
                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="SearchKpiElapsedTitle" Text="所要時間:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="SearchKpiElapsedText" Text="0.00s" FontSize="14" FontWeight="Bold" Typography.NumeralAlignment="Tabular" Foreground="#059669" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Separator 3 -->
                        <Border Grid.Column="5" Width="1" Background="#E2E8F0" Margin="14,2"/>

                        <!-- Status / Path -->
                        <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="SearchStatusTitle" Text="走査状況:" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="SearchStatusText" Text="待機中" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="#64748B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Results DataGrid -->
                <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock x:Name="SearchTableTitleText" Text="検索結果一覧" FontWeight="Bold" FontSize="12" Foreground="#0F172A" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SearchTableHintText" Text="" Visibility="Collapsed"/>
                        </Grid>
                        <DataGrid x:Name="SearchDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="28"
                                  HeadersVisibility="Column" RowHeaderWidth="0"
                                  VirtualizingStackPanel.IsVirtualizing="True"
                                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                                  EnableRowVirtualization="True"
                                  ScrollViewer.CanContentScroll="True"
                                  MouseDoubleClick="SearchDataGrid_MouseDoubleClick">
                            <DataGrid.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="📄 ファイルを開く (Open)" Click="SearchContextMenu_Open_Click"/>
                                    <MenuItem Header="📂 エクスプローラーで表示 (Explore)" Click="SearchContextMenu_Explore_Click"/>
                                    <MenuItem Header="📋 フルパスをコピー (Copy Path)" Click="SearchContextMenu_CopyPath_Click"/>
                                    <MenuItem Header="📋 ファイル名をコピー (Copy Name)" Click="SearchContextMenu_CopyName_Click"/>
                                    <Separator/>
                                    <MenuItem Header="🛡️ Live ACLで開く (Live ACL Control)" Click="SearchContextMenu_LiveAcl_Click"/>
                                    <MenuItem Header="🚀 移行スタジオに追加 (Simulation)" Click="SearchContextMenu_Simulation_Click"/>
                                    <MenuItem Header="🔗 リンク修復へ転送 (LinkFixer)" Click="SearchContextMenu_LinkFix_Click"/>
                                    <MenuItem Header="🧹 ファイル整理・監査へ転送 (Audit &amp; Hygiene)" Click="SearchContextMenu_Audit_Click"/>
                                    <Separator/>
                                    <MenuItem Header="📊 検索結果をExcelでエクスポート (.xlsx)" Click="SearchExportExcelButton_Click"/>
                                    <MenuItem Header="📑 検索結果をCSVでエクスポート (.csv)" Click="SearchExportCsvButton_Click"/>
                                </ContextMenu>
                            </DataGrid.ContextMenu>
                            <DataGrid.Columns>
                                <DataGridTextColumn x:Name="ColSearchType" Header="種別" Binding="{Binding TypeIcon}" Width="50" ElementStyle="{StaticResource CenterAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchName" Header="ファイル名" Binding="{Binding Name}" SortMemberPath="Name" Width="220" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchSize" Header="容量" Binding="{Binding FormattedSize}" SortMemberPath="SizeBytes" Width="95" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchModified" Header="最終更新日時" Binding="{Binding FormattedDate}" SortMemberPath="LastWriteTime" Width="135" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchExt" Header="拡張子" Binding="{Binding Extension}" SortMemberPath="Extension" Width="65" ElementStyle="{StaticResource CenterAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchPathLen" Header="文字数" Binding="{Binding PathLength}" SortMemberPath="PathLength" Width="60" ElementStyle="{StaticResource RightAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchMatch" Header="一致理由 / 抜粋" Binding="{Binding DisplaySnippetOrReason}" SortMemberPath="MatchedReason" Width="220" ElementStyle="{StaticResource LeftAlignedCell}"/>
                                <DataGridTextColumn x:Name="ColSearchFullPath" Header="完全パス" Binding="{Binding FullPath}" SortMemberPath="FullPath" Width="*" ElementStyle="{StaticResource LeftAlignedCell}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

        <!-- ======================================================== -->
        <!-- MODAL 1: WINDOWS SECURITY DETAILED PERMISSION MODAL      -->
        <!-- ======================================================== -->
        <Grid x:Name="SecModalOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="680" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="*"/>    <!-- Body (Basic + Adv 14 items) -->
                        <RowDefinition Height="Auto"/> <!-- Footer Buttons -->
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="SecModalTitleText" Text="🛡️ セキュリティの詳細設定 - " FontWeight="Bold" FontSize="14" Foreground="#0F172A"/>
                            <TextBlock x:Name="SecModalTargetNameText" Text="フォルダ名" FontWeight="Bold" FontSize="14" Foreground="#0284C7"/>
                        </StackPanel>
                        <Button Content="✕" Width="24" Height="24" Background="Transparent" BorderThickness="0"
                                FontSize="12" Foreground="#64748B" HorizontalAlignment="Right" Click="SecModalCancel_Click"/>
                    </Grid>

                    <ScrollViewer Grid.Row="1" MaxHeight="520" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Object Path -->
                            <TextBlock x:Name="SecModalObjectNameLabel" Text="オブジェクト名:" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,0,10">
                                <TextBlock x:Name="SecModalFullPathText" Text="\\NewServer01\Share\Folder" FontFamily="{StaticResource MonoFont}" FontSize="11" Foreground="#0369A1"/>
                            </Border>

                            <!-- Principal & Applies To -->
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="140"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock x:Name="SecModalPrincipalLabel" Text="プリンシパル (対象アカウント):" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <TextBox x:Name="SecPrincipalInput" Height="28" FontSize="11" FontWeight="Bold" VerticalContentAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="SecModalTypeLabel" Text="種類:" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <ComboBox x:Name="SecAccessTypeCombo" Height="28" SelectedIndex="0" VerticalContentAlignment="Center">
                                        <ComboBoxItem x:Name="SecComboItemAllow" Content="許可"/>
                                        <ComboBoxItem x:Name="SecComboItemDeny" Content="拒否"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,0,0,12">
                                <TextBlock x:Name="SecModalAppliesToLabel" Text="適用先:" FontSize="11" Foreground="#64748B" Margin="0,0,0,2"/>
                                <ComboBox x:Name="SecAppliesToCombo" Height="28" SelectedIndex="0" VerticalContentAlignment="Center">
                                    <ComboBoxItem x:Name="SecComboAppliesToAll" Content="このフォルダー、サブフォルダーおよびファイル"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToFolderOnly" Content="このフォルダーのみ"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToFolderAndSub" Content="このフォルダーおよびサブフォルダー"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToFolderAndFiles" Content="このフォルダーおよびファイル"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToSubAndFiles" Content="サブフォルダーおよびファイルのみ"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToSubOnly" Content="サブフォルダーのみ"/>
                                    <ComboBoxItem x:Name="SecComboAppliesToFilesOnly" Content="ファイルのみ"/>
                                </ComboBox>
                            </StackPanel>

                            <!-- Basic Permissions -->
                            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock x:Name="SecModalBasicPermTitle" Text="基本アクセス許可:" FontWeight="Bold" FontSize="12.5" Foreground="#334155"/>
                                        <TextBlock x:Name="SecModalRealtimeNotice" Text="" Visibility="Collapsed"/>
                                    </Grid>
                                    <UniformGrid Columns="2">
                                        <CheckBox x:Name="SecChkFullControl" Content="フル コントロール" Margin="0,3" FontWeight="Bold" Click="SecBasicPerm_Click"/>
                                        <CheckBox x:Name="SecChkModify" Content="変更 (Modify)" Margin="0,3" FontWeight="Bold" Click="SecBasicPerm_Click"/>
                                        <CheckBox x:Name="SecChkReadExecute" Content="読み取りと実行" Margin="0,3" Click="SecBasicPerm_Click"/>
                                        <CheckBox x:Name="SecChkList" Content="フォルダーの内容の一覧表示" Margin="0,3" Click="SecBasicPerm_Click"/>
                                        <CheckBox x:Name="SecChkRead" Content="読み取り" Margin="0,3" Click="SecBasicPerm_Click"/>
                                        <CheckBox x:Name="SecChkWrite" Content="書き込み" Margin="0,3" Click="SecBasicPerm_Click"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <!-- Advanced Permissions (14 items) -->
                            <Border Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="12">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock x:Name="SecModalAdvPermTitle" Text="⚙️ 高度なアクセス許可:" FontWeight="Bold" FontSize="12" Foreground="#0284C7"/>
                                        <TextBlock x:Name="SecModalAdvPermSubtitle" Text="" Visibility="Collapsed"/>
                                    </Grid>
                                    <UniformGrid Columns="2">
                                        <CheckBox x:Name="SecAdvTraverse" Content="フォルダーのスキャン / ファイルの実行" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvList" Content="フォルダーの一覧 / データの読み取り" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvReadAttr" Content="属性の読み取り" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvReadExtAttr" Content="拡張属性の読み取り" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvCreateFile" Content="ファイルの作成 / データの書き込み" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvCreateFolder" Content="フォルダーの作成 / データの追加" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvWriteAttr" Content="属性の書き込み" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvWriteExtAttr" Content="拡張属性の書き込み" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvDelete" Content="削除" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvDeleteSub" Content="サブフォルダーとファイルの削除" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvReadPerm" Content="アクセス許可の読み取り" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvChangePerm" Content="アクセス許可の変更" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvTakeOwnership" Content="所有権の取得" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                        <CheckBox x:Name="SecAdvSync" Content="同期 (Synchronize)" Margin="0,3" FontSize="11" Click="SecAdvPerm_Click"/>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                        <Button x:Name="SecModalCancelButton" Content="キャンセル" Height="32" Width="90" Margin="0,0,8,0" Style="{StaticResource FluentButtonSecondary}" Click="SecModalCancel_Click"/>
                        <Button x:Name="SecModalApplyButton" Content="変更を保存" Height="32" Width="100" Style="{StaticResource FluentButtonPrimary}" Click="SecModalApply_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ======================================================== -->
        <!-- MODAL: ENTERPRISE MIGRATION PACKAGE GENERATOR (RUNBOOK)  -->
        <!-- ======================================================== -->
        <Grid x:Name="MigrationPackageOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="1040" Height="700" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="12" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- 0: Header -->
                        <RowDefinition Height="Auto"/> <!-- 1: Config Cards (Policy & Options) -->
                        <RowDefinition Height="Auto"/> <!-- 2: KPI Cards -->
                        <RowDefinition Height="*"/>    <!-- 3: Wave Preview Table -->
                        <RowDefinition Height="Auto"/> <!-- 4: Footer -->
                    </Grid.RowDefinitions>

                    <!-- Row 0: Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock x:Name="MigPkgTitleText" Text="📦 移行パッケージ生成 (Runbook &amp; バッチ一式)" FontWeight="Bold" FontSize="16" Foreground="#0F172A"/>
                                <Border Background="#EFF6FF" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="ベンダー標準 移行プロセス" FontSize="10.5" FontWeight="SemiBold" Foreground="#1D4ED8"/>
                                </Border>
                            </StackPanel>
                            <TextBlock x:Name="MigPkgSubtitleText" Text="事前フル同期、中間差分、旧共有封鎖、本番切替、切戻しの各工程バッチと、進捗管理Excel台帳 (Runbook) を一括生成します。"
                                       FontSize="11.5" Foreground="#64748B" Margin="0,3,0,0"/>
                        </StackPanel>
                        <Button Content="✕" Width="26" Height="26" Background="Transparent" BorderThickness="0"
                                FontSize="13" Foreground="#64748B" HorizontalAlignment="Right" VerticalAlignment="Top"
                                Click="MigPkgModalClose_Click" Cursor="Hand"/>
                    </Grid>

                    <!-- Row 1: Config Cards (Two Columns) -->
                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Card: Split Policy -->
                        <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12,10">
                            <StackPanel>
                                <TextBlock x:Name="MigPolicyHeaderLabel" Text="1. 移行波次 (Wave) 分割ポリシー" FontWeight="Bold" FontSize="12" Foreground="#1E293B" Margin="0,0,0,6"/>
                                <RadioButton x:Name="MigPolicyTopLevelRadio" GroupName="MigPolicy" IsChecked="True" Margin="0,2,0,4"
                                             Checked="MigPolicyRadio_Checked">
                                    <TextBlock x:Name="MigPolicyTopLevelText" Text="トップフォルダ・部署ごとにWave分割 (推奨: 業務影響最小化)" FontSize="11" Foreground="#334155"/>
                                </RadioButton>

                                <RadioButton x:Name="MigPolicySizeBudgetRadio" GroupName="MigPolicy" Margin="0,2,0,4"
                                             Checked="MigPolicyRadio_Checked">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="MigPolicySizeBudgetText" Text="容量バジェットごと (指定サイズ以下に自動集約):" FontSize="11" Foreground="#334155" VerticalAlignment="Center"/>
                                        <TextBox x:Name="MigSizeBudgetText" Text="500" Width="45" Height="22" Margin="6,0,4,0" FontSize="11"
                                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                                 BorderBrush="#CBD5E1" Background="#FFFFFF" TextChanged="MigSizeBudgetText_TextChanged"/>
                                        <TextBlock Text="GB" FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton x:Name="MigPolicySingleBatchRadio" GroupName="MigPolicy" Margin="0,2,0,0"
                                             Checked="MigPolicyRadio_Checked">
                                    <TextBlock x:Name="MigPolicySingleBatchText" Text="一括出力 (全フォルダを単一バッチに集約)" FontSize="11" Foreground="#334155"/>
                                </RadioButton>
                            </StackPanel>
                        </Border>

                        <!-- Right Card: Transfer Options & Safety -->
                        <Border Grid.Column="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12,10">
                            <StackPanel>
                                <TextBlock x:Name="MigOptionsHeaderLabel" Text="2. 転送オプション &amp; 安全弁" FontWeight="Bold" FontSize="12" Foreground="#1E293B" Margin="0,0,0,6"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,4">
                                    <RadioButton x:Name="MigModeCopyDatRadio" GroupName="MigTransferMode" IsChecked="True" Margin="0,0,12,0">
                                        <TextBlock x:Name="MigModeCopyDatText" Text="新設計ACL維持 (/COPY:DAT 推奨)" FontSize="11" Foreground="#334155"/>
                                    </RadioButton>
                                    <RadioButton x:Name="MigModeCopyAllRadio" GroupName="MigTransferMode">
                                        <TextBlock x:Name="MigModeCopyAllText" Text="旧ACL維持 (/COPYALL)" FontSize="11" Foreground="#334155"/>
                                    </RadioButton>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,1,0,4">
                                    <TextBlock x:Name="MigSpeedLabel" Text="想定速度:" FontSize="11" Foreground="#334155" VerticalAlignment="Center"/>
                                    <TextBox x:Name="MigSpeedText" Text="80" Width="38" Height="22" Margin="4,0,2,0" FontSize="11"
                                             VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                             BorderBrush="#CBD5E1" Background="#FFFFFF" TextChanged="MigParamText_TextChanged"/>
                                    <TextBlock Text="MB/s" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                    <TextBlock x:Name="MigDeltaRatioLabel" Text="想定差分率:" FontSize="11" Foreground="#334155" VerticalAlignment="Center"/>
                                    <TextBox x:Name="MigDeltaRatioText" Text="2.0" Width="38" Height="22" Margin="4,0,2,0" FontSize="11"
                                             VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                             BorderBrush="#CBD5E1" Background="#FFFFFF" TextChanged="MigParamText_TextChanged"/>
                                    <TextBlock Text="%" FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                                </StackPanel>

                                <CheckBox x:Name="MigIncludeExcelCheck" Content="進捗管理Excel手順書 (Migration_Runbook.xlsx) を同梱"
                                          IsChecked="True" FontSize="11" Foreground="#334155" Margin="0,2,0,2"/>
                                <CheckBox x:Name="MigIncludeLockCheck" Content="旧共有の安全停止ガイド (03_PreCutover_Freeze_Guide.md) を同梱"
                                          IsChecked="True" FontSize="11" Foreground="#334155" Margin="0,0,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Row 2: 4-KPI Strip -->
                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Card 1: Total Size -->
                        <Border Grid.Column="0" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10,6">
                            <StackPanel>
                                <TextBlock x:Name="MigKpiTotalSizeLabel" Text="総移行データ量" FontSize="10" Foreground="#64748B"/>
                                <TextBlock x:Name="MigKpiTotalSizeVal" Text="0 B" FontSize="14" FontWeight="Bold" Foreground="#0F172A" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Card 2: Total Files -->
                        <Border Grid.Column="2" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10,6">
                            <StackPanel>
                                <TextBlock x:Name="MigKpiTotalFilesLabel" Text="総ファイル件数" FontSize="10" Foreground="#64748B"/>
                                <TextBlock x:Name="MigKpiTotalFilesVal" Text="0 件" FontSize="14" FontWeight="Bold" Foreground="#0F172A" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Card 3: Est. Full Copy -->
                        <Border Grid.Column="4" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="6" Padding="10,6">
                            <StackPanel>
                                <TextBlock x:Name="MigKpiFullTimeLabel" Text="初回フル同期想定 (1Gbps)" FontSize="10" Foreground="#1E40AF"/>
                                <TextBlock x:Name="MigKpiFullTimeVal" Text="0s" FontSize="14" FontWeight="Bold" Foreground="#1D4ED8" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Card 4: Est. Cutover -->
                        <Border Grid.Column="6" Background="#ECFDF5" BorderBrush="#A7F3D0" BorderThickness="1" CornerRadius="6" Padding="10,6">
                            <StackPanel>
                                <TextBlock x:Name="MigKpiCutoverTimeLabel" Text="本番切替想定 (差分2%)" FontSize="10" Foreground="#065F46"/>
                                <TextBlock x:Name="MigKpiCutoverTimeVal" Text="0s" FontSize="14" FontWeight="Bold" Foreground="#059669" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Row 3: Wave Preview Table -->
                    <Border Grid.Row="3" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="2">
                        <DataGrid x:Name="MigWavePlanDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="32"
                                  SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Wave" Binding="{Binding WaveNumber, StringFormat='Wave {0}'}" Width="75">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="#1E293B"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="波次名称・移行対象" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding WaveName}" FontWeight="SemiBold" FontSize="11.5" Foreground="#0F172A"
                                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding WaveName}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="想定容量" Binding="{Binding TotalSizeFormatted}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="ファイル数" Binding="{Binding TotalFileCountFormatted}" Width="95">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="初回フル同期 (1Gbps)" Binding="{Binding FullCopyTimeFormatted}" Width="130">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#1D4ED8"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="本番切替 (差分2%)" Binding="{Binding CutoverTimeFormatted}" Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="#059669"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Header="警告・判定" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Border Background="#FEF3C7" CornerRadius="3" Padding="4,1" Margin="2,0"
                                                        Visibility="{Binding HasOver48hWarning, Converter={StaticResource BoolToVis}}"
                                                        ToolTip="初回フルコピーが48時間超の見込みです。週末枠超過に注意してください。">
                                                    <TextBlock Text="⚠️ 48h超" FontSize="9.5" FontWeight="Bold" Foreground="#B45309"/>
                                                </Border>
                                                <Border Background="#FEE2E2" CornerRadius="3" Padding="4,1" Margin="2,0"
                                                        Visibility="{Binding HasHighFileCountWarning, Converter={StaticResource BoolToVis}}"
                                                        ToolTip="ファイル数が10万件超です。ランダムI/O過多のためスレッド数(/MT:32)の増強を推奨します。">
                                                    <TextBlock Text="⚠️ ファイル過多" FontSize="9.5" FontWeight="Bold" Foreground="#B91C1C"/>
                                                </Border>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Row 4: Footer -->
                    <Grid Grid.Row="4" Margin="0,14,0,0">
                        <TextBlock x:Name="MigFooterNoticeText" Text="※ 子孫フォルダ (/XD) は自動除外されます。出力フォルダにバッチ・手順書一式を生成します。"
                                   FontSize="11" Foreground="#64748B" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="MigPkgCancelButton" Content="キャンセル" Height="32" Width="85" Margin="0,0,8,0"
                                    Style="{StaticResource FluentButtonSecondary}" Click="MigPkgModalClose_Click"/>
                            <Button x:Name="MigPkgExportButton" Content="📦 移行パッケージ一式を出力" Height="32" Padding="14,0"
                                    Style="{StaticResource FluentButtonPrimary}" Click="MigPkgExportButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- ======================================================== -->
        <!-- MODAL 2: HUMAN-FRIENDLY DIFF REVIEW MODAL                -->
        <!-- ======================================================== -->
        <Grid x:Name="DiffModalOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="980" Height="640" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="Auto"/> <!-- Subtitle -->
                        <RowDefinition Height="*"/>    <!-- Diff Table -->
                        <RowDefinition Height="Auto"/> <!-- Footer -->
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="DiffModalTitleText" Text="⚖️ 変更点" FontWeight="Bold" FontSize="15" Foreground="#0F172A"/>
                            <TextBlock x:Name="DiffModalSubTitleText" Text=" - 移行設計差分" FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                        <Button Content="✕" Width="24" Height="24" Background="Transparent" BorderThickness="0"
                                FontSize="12" Foreground="#64748B" HorizontalAlignment="Right" Click="DiffModalClose_Click"/>
                    </Grid>

                    <!-- Summary Stats Strip -->
                    <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="DiffSummaryStatsText" Text="📊 差分項目: 0件" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Diff DataGrid -->
                    <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="2">
                        <DataGrid x:Name="DiffReviewDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="32"
                                  SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn x:Name="ColDiffType" Header="変化の種別" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:SimDiffItem}">
                                            <Border Background="{Binding DiffTypeBadgeBackground}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding DiffType}" FontWeight="Bold" FontSize="10" Foreground="{Binding DiffTypeBadgeForeground}"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColDiffSource" Header="現行サーバー (Before)" Width="280">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:SimDiffItem}">
                                            <StackPanel Margin="6,4">
                                                <TextBlock Text="{Binding SourcePath}" FontWeight="Bold" FontSize="11" Foreground="#334155" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding SourceDetail}" FontSize="10" Foreground="#64748B" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColDiffTarget" Header="新環境設計 (After)" Width="280">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:SimDiffItem}">
                                            <StackPanel Margin="6,4">
                                                <TextBlock Text="{Binding TargetPath}" FontWeight="Bold" FontSize="11" Foreground="#0284C7" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding TargetDetail}" FontSize="10" Foreground="#64748B" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColDiffAcl" Header="権限 (ACL) 差分詳細" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type models:SimDiffItem}">
                                            <TextBlock Text="{Binding FormattedAclChanges}" FontSize="11" Foreground="#1E293B" Margin="6,4" TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button x:Name="DiffModalCloseButton" Content="閉じる" Height="32" Width="80" Margin="0,0,8,0" Style="{StaticResource FluentButtonSecondary}" Click="DiffModalClose_Click"/>
                        <Button x:Name="DiffExportExcelButton" Content="📊 Excel出力" Height="32" Margin="0,0,8,0" Style="{StaticResource FluentButtonSecondary}" Click="DiffExportExcel_Click"/>
                        <Button x:Name="DiffModalApplyButton" Content="⚡ 適用" Height="32" Style="{StaticResource FluentButtonPrimary}" Click="DiffModalApply_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ======================================================== -->
        <!-- MODAL: LINK FIX DRY-RUN DIFF MODAL                       -->
        <!-- ======================================================== -->
        <Grid x:Name="LinkFixDiffModalOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="920" Height="560" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="Auto"/> <!-- Summary Strip -->
                        <RowDefinition Height="*"/>    <!-- Diff Table -->
                        <RowDefinition Height="Auto"/> <!-- Footer -->
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="LinkFixDiffTitleText" Text="⚖️ 変更点" FontWeight="Bold" FontSize="15" Foreground="#0F172A"/>
                            <TextBlock x:Name="LinkFixDiffSubTitleText" Text=" - ショートカット一括修復" FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                        <Button Content="✕" Width="24" Height="24" Background="Transparent" BorderThickness="0"
                                FontSize="12" Foreground="#64748B" HorizontalAlignment="Right" Click="LinkFixDiffModalClose_Click"/>
                    </Grid>

                    <!-- Summary Stats Strip -->
                    <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="LinkFixDiffSummaryText" Text="📊 修復対象: 0件 (各ファイル .bak 自動バックアップ生成)" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Diff DataGrid -->
                    <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="2">
                        <DataGrid x:Name="LinkFixDiffDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="30"
                                  SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn x:Name="ColLinkDiffFileName" Header="ファイル名" Binding="{Binding FileName}" Width="200"/>
                                <DataGridTemplateColumn x:Name="ColLinkDiffOldTarget" Header="置換前ターゲット (Before)" Width="320">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding OldTarget}" FontSize="11" Foreground="#64748B" TextTrimming="CharacterEllipsis" ToolTip="{Binding OldTarget}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn x:Name="ColLinkDiffNewTarget" Header="置換後ターゲット (After)" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding NewTarget}" FontWeight="SemiBold" FontSize="11" Foreground="#0284C7" TextTrimming="CharacterEllipsis" ToolTip="{Binding NewTarget}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button x:Name="LinkFixDiffModalCloseButton" Content="閉じる" Height="32" Width="80" Margin="0,0,8,0" Style="{StaticResource FluentButtonSecondary}" Click="LinkFixDiffModalClose_Click"/>
                        <Button x:Name="LinkFixDiffModalApplyButton" Content="⚡ 適用" Height="32" Style="{StaticResource FluentButtonPrimary}" Click="LinkFixDiffModalApply_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ======================================================== -->
        <!-- MODAL: MEDIA OPTIMIZER DRY-RUN DIFF MODAL                -->
        <!-- ======================================================== -->
        <Grid x:Name="MediaDiffModalOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="920" Height="560" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="10" BorderBrush="#CBD5E1" BorderThickness="1" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="Auto"/> <!-- Summary Strip -->
                        <RowDefinition Height="*"/>    <!-- Diff Table -->
                        <RowDefinition Height="Auto"/> <!-- Footer -->
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="MediaDiffTitleText" Text="⚖️ 変更点" FontWeight="Bold" FontSize="15" Foreground="#0F172A"/>
                            <TextBlock x:Name="MediaDiffSubTitleText" Text=" - 写真・画像軽量化 (上書き縮小)" FontSize="12" Foreground="#64748B" VerticalAlignment="Center" Margin="6,0,0,0"/>
                        </StackPanel>
                        <Button Content="✕" Width="24" Height="24" Background="Transparent" BorderThickness="0"
                                FontSize="12" Foreground="#64748B" HorizontalAlignment="Right" Click="MediaDiffModalClose_Click"/>
                    </Grid>

                    <!-- Summary Stats Strip -->
                    <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="MediaDiffSummaryText" Text="📊 対象: 0枚 / 設定: 長辺 2560px・品質 85% (🛡️ 保護対象除外済)" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Diff DataGrid -->
                    <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="2">
                        <DataGrid x:Name="MediaDiffDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                                  BorderThickness="0" Background="Transparent" GridLinesVisibility="None" RowHeight="30"
                                  SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn x:Name="ColMediaDiffFileName" Header="ファイル名" Binding="{Binding FileName}" Width="200"/>
                                <DataGridTextColumn x:Name="ColMediaDiffOriginalSize" Header="元容量" Binding="{Binding OriginalSizeFormatted}" Width="110"/>
                                <DataGridTextColumn x:Name="ColMediaDiffDimensions" Header="現在の解像度" Binding="{Binding DimensionsFormatted}" Width="130"/>
                                <DataGridTemplateColumn x:Name="ColMediaDiffFullPath" Header="完全パス" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FullPath}" FontSize="11" Foreground="#64748B" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullPath}" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                        <Button x:Name="MediaDiffModalCloseButton" Content="閉じる" Height="32" Width="80" Margin="0,0,8,0" Style="{StaticResource FluentButtonSecondary}" Click="MediaDiffModalClose_Click"/>
                        <Button x:Name="MediaDiffModalApplyButton" Content="⚡ 適用" Height="32" Style="{StaticResource FluentButtonPrimary}" Click="MediaDiffModalApply_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>


        <!-- ======================================================== -->
        <!-- MODAL 3: SETTINGS MODAL (環境設定: キャッシュ共有・保存先)   -->
        <!-- ======================================================== -->
        <Grid x:Name="SettingsModalOverlay" Grid.RowSpan="3" Background="#80000000" Visibility="Collapsed">
            <Border Background="#FFFFFF" Width="640" VerticalAlignment="Center" HorizontalAlignment="Center"
                    CornerRadius="12" BorderBrush="#CBD5E1" BorderThickness="1" Padding="24">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#000000" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Title -->
                        <RowDefinition Height="Auto"/> <!-- Description -->
                        <RowDefinition Height="*"/>    <!-- Settings Body -->
                        <RowDefinition Height="Auto"/> <!-- Actions -->
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock x:Name="SettingsTitleText" Text="⚙️ 環境設定 (Settings)" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock x:Name="SettingsDescText" Text="キャッシュ、スナップショット履歴、監査レポートの参照先および保存先を構成します。" FontSize="11.5" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Settings Body -->
                    <StackPanel Grid.Row="2" Margin="0,4,0,16">
                        <!-- Read Cache Card -->
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock x:Name="SettingsReadTitleText" Text="📂 キャッシュ・スナップショット 参照先 (読み込み)" FontWeight="Bold" FontSize="12.5" Foreground="#1E293B"/>
                                <TextBlock x:Name="SettingsReadDescText" Text="共有ファイルサーバー上のマスターキャッシュ（UNCパス等）を指定すると、チーム共通の0秒ツリーや推移履歴を参照できます。" FontSize="11" Foreground="#64748B" Margin="0,2,0,8" TextWrapping="Wrap"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="SettingsReadPathTextBox" Height="32" VerticalContentAlignment="Center" Padding="8,0" FontSize="12"
                                             Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1"
                                             ToolTip="空の場合はローカル既定値 (%LocalAppData%\FolderMorpher) を参照します"/>
                                    <Button x:Name="SettingsBrowseReadButton" Grid.Column="1" Content="参照..." Margin="8,0,0,0" Width="70" Height="32"
                                            Style="{StaticResource FluentButtonSecondary}" Click="SettingsBrowseReadButton_Click"/>
                                </Grid>
                                <CheckBox x:Name="SettingsFallbackCheckBox" Content="共有参照先にアクセスできない場合は自動でローカルキャッシュを参照する"
                                          IsChecked="True" FontSize="11.5" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Write Cache Card -->
                        <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="14">
                            <StackPanel>
                                <TextBlock x:Name="SettingsWriteTitleText" Text="💾 キャッシュ・スナップショット 保存先 (書き込み)" FontWeight="Bold" FontSize="12.5" Foreground="#1E293B"/>
                                <TextBlock x:Name="SettingsWriteDescText" Text="自身がスキャンした結果のツリーキャッシュおよび履歴データの保存場所を選択します。" FontSize="11" Foreground="#64748B" Margin="0,2,0,8" TextWrapping="Wrap"/>

                                <RadioButton x:Name="SettingsWriteModeLocalRadio" GroupName="WriteMode" IsChecked="True" Margin="0,2,0,4"
                                             Checked="SettingsWriteModeRadio_Checked">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="SettingsWriteLocalText" Text="ローカルに保存" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B"/>
                                        <TextBlock x:Name="SettingsWriteLocalSubText" Text=" (推奨: マスターキャッシュを上書きしない安全設定)" FontSize="11" Foreground="#64748B"/>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton x:Name="SettingsWriteModeSameRadio" GroupName="WriteMode" Margin="0,2,0,4"
                                             Checked="SettingsWriteModeRadio_Checked">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="SettingsWriteSameText" Text="参照先と同じフォルダーに保存" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B"/>
                                        <TextBlock x:Name="SettingsWriteSameSubText" Text=" (管理者・マスター更新者用)" FontSize="11" Foreground="#64748B"/>
                                    </StackPanel>
                                </RadioButton>

                                <RadioButton x:Name="SettingsWriteModeCustomRadio" GroupName="WriteMode" Margin="0,2,0,4"
                                             Checked="SettingsWriteModeRadio_Checked">
                                    <TextBlock x:Name="SettingsWriteCustomText" Text="任意のカスタムフォルダーを指定" FontWeight="SemiBold" FontSize="12" Foreground="#1E293B"/>
                                </RadioButton>

                                <Grid x:Name="SettingsCustomPathGrid" Margin="22,6,0,0" IsEnabled="False">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="SettingsCustomPathTextBox" Height="30" VerticalContentAlignment="Center" Padding="8,0" FontSize="11.5"
                                             Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1"/>
                                    <Button x:Name="SettingsBrowseCustomButton" Grid.Column="1" Content="参照..." Margin="8,0,0,0" Width="70" Height="30"
                                            Style="{StaticResource FluentButtonSecondary}" Click="SettingsBrowseCustomButton_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Actions -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SettingsCancelButton" Content="キャンセル" Width="90" Height="32" Margin="0,0,8,0"
                                Style="{StaticResource FluentButtonSecondary}" Click="SettingsCancelButton_Click"/>
                        <Button x:Name="SettingsSaveButton" Content="設定を保存" Width="100" Height="32"
                                Style="{StaticResource FluentButtonPrimary}" Click="SettingsSaveButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

            <!-- TOAST NOTIFICATION -->
            <Border x:Name="ToastNotificationBorder" Background="#0F172A" CornerRadius="6" Padding="14,10"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,24,24" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="4" Direction="270" Color="#000000" Opacity="0.3"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💡 " FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ToastNotificationText" Text="通知メッセージ" FontSize="12" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- GLOBAL FOOTER / STATUS BAR -->
        <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusTextBlock" Text="準備完了" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                    <ProgressBar x:Name="GlobalProgressBar" Height="4" Width="140" Margin="12,0,0,0" Minimum="0" Maximum="100" Value="0"
                                 Visibility="Collapsed" Foreground="{StaticResource AccentSky}" Background="#F1F5F9" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
    </Grid>
</Window>
